import { ref, computed } from 'vue'
import { defineStore } from 'pinia'

export const useParticipantStore = defineStore('participant', () => {
  // Participant List
  // Name
  // Status - Contacted, Ready, Revoked,
  // Class - Alternative, Primary
  // Assigned Team - A, B, Unassigned
  return {}
})
